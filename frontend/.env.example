# HEIRS frontend environment template
# Copy to .env to override values for local development.
# The backend REST API base URL. Defaults to the local Spring Boot server.
VITE_API_BASE_URL=http://127.0.0.1:8080