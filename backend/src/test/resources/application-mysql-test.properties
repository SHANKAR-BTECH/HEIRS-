# Explicit opt-in: this schema is destroyed and recreated by tests. Never point at heirs_db.
spring.datasource.url=${TEST_DB_URL:jdbc:mysql://127.0.0.1:3307/heirs_test}
spring.datasource.username=${DB_USERNAME:heirs}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=create-drop
heirs.seed.enabled=false
